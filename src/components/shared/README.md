# components/shared

Cross-cutting components used by more than one route group, but not
generic enough to belong in `components/ui` (e.g. a shared page header
that differs slightly from a pure design-system primitive). No
components implemented yet — Phase 1 is repository foundation only.
