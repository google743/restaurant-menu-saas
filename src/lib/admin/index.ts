/**
 * lib/admin
 * -----------------------------------------------------------------------
 * Boundary for data access, server actions, and helpers used by the
 * (admin) route group (internal/administrative surface). Empty in
 * Phase 1 — repository foundation only.
 */
export {};
