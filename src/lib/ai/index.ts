/**
 * lib/ai
 * -----------------------------------------------------------------------
 * Boundary for AI-assisted features (e.g. menu content generation,
 * suggestions). Empty in Phase 1 — repository foundation only. No
 * provider SDK is installed yet; add it here when AI features are
 * implemented in a later phase.
 */
export {};
