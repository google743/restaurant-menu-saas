/**
 * lib/auth
 * -----------------------------------------------------------------------
 * Boundary for authentication and authorization: sign-in/sign-up flows,
 * session helpers, and route protection built on top of
 * `lib/supabase`. Empty in Phase 1 — authentication is explicitly
 * excluded from this phase and implemented in the auth phase.
 */
export {};
