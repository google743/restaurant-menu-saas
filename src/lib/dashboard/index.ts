/**
 * lib/dashboard
 * -----------------------------------------------------------------------
 * Boundary for data access, server actions, and helpers used by the
 * (dashboard) route group (authenticated business/owner surface). Empty
 * in Phase 1 — repository foundation only.
 */
export {};
