/**
 * lib/public
 * -----------------------------------------------------------------------
 * Boundary for data access and helpers used by the (public) route group
 * (unauthenticated, public-facing pages). Empty in Phase 1 — repository
 * foundation only.
 */
export {};
