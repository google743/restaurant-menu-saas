/**
 * lib/qr
 * -----------------------------------------------------------------------
 * Boundary for QR code generation (e.g. per-branch/per-menu QR codes).
 * Empty in Phase 1 — repository foundation only. No QR generation
 * library is installed yet; add it here when this feature is
 * implemented in a later phase.
 */
export {};
