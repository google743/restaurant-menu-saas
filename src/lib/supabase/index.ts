/**
 * lib/supabase
 * -----------------------------------------------------------------------
 * Supabase client boundary. `./client` for Client Components, `./server`
 * for Server Components / Server Actions / Route Handlers. Import the
 * specific module you need rather than this barrel, so bundlers can tell
 * browser and server code apart.
 *
 * Phase 1 scope: client construction only. No schema, no RLS, no auth
 * flows yet.
 */
export {};
