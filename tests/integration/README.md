# tests/integration

Integration tests (e.g. against a local Supabase instance, route
handlers). Empty in Phase 1 — there's no database or business logic yet
to integration-test. Runs through the same Vitest config as
`tests/unit`.
